import * as cssCodes from './cssCodes';
import * as values from './values';
import colors from './colors';

export const s = {
  ...cssCodes,
  ...values,
  ...colors,
};

export default s;

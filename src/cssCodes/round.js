import { ROUND } from '../values';

const round = {
  xs: `${ROUND.xs}px;`,
  sm: `${ROUND.sm}px;`,
  md: `${ROUND.md}px;`,
  lg: `${ROUND.lg}px;`,
  xl: `${ROUND.xl}px;`,
};

export default round;
